#include "main.h"
#include <stdio.h>

/**
 * _strcat - concatenates two strings.
 * @src: source.
 * @dest: destination.
 * Return: the point to dest
 */
char *_strcat(char *dest, char *src)
{
	char dest[] = "Hello ";
	char src[] = "World";

	strcat(dest, source);
}
