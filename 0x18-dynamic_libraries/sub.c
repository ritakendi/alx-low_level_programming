#include "main.h"
/**
 * add - adds two integers and returns the results
 *
 * @a: first number
 * @b: second number
 * Return: substraction of two integers.
 */

int sub(int a, int b)
{

	{
		return (a - b);
	}
}