#include "main.h"
/**
 * mul - multiplies two integers and returns the results
 *
 * @a: first number
 * @b: second number
 * Return: multiplication of two integers.
 */

int mul(int a, int b)
{

	{
		return (a * b);
	}
}