#include "main.h"
/**
 * div - divides two integers and returns the results
 *
 * @a: first number
 * @b: second number
 * Return: division of two integers.
 */

int div(int a, int b)
{

	{
		return (a / b);
	}
}