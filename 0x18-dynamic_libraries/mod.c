#include "main.h"
/**
 * mod - modules two integers and returns the results
 *
 * @a: first number
 * @b: second number
 * Return: mod of two integers.
 */

int mod(int a, int b)
{

	{
		return (a % b);
	}
}