#include "main.h"
/**
 * add - modules two integers and returns the results
 *
 * @a: first number
 * @b: second number
 * Return: mod of two integers.
 */

int add(int a, int b)
{

	{
		return (a % b);
	}
}