#include "main.h"
/**
 * add - divides two integers and returns the results
 *
 * @a: first number
 * @b: second number
 * Return: division of two integers.
 */

int add(int a, int b)
{

	{
		return (a / b);
	}
}