#include "main.h"

/**
 * main - main entry
 *
 * Return: Always 0
 */

int main(void)
{
	void print_alphabet(void);

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar(alpha);
	}
	_putchar('\n');
}
